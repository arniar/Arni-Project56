<!-- views/profile.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ARNI - Profile Pages</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.css" />
    <link rel="stylesheet" href="/stylesheets/user/profile.css">
</head>
<body>
    <canvas id="backgroundCanvas"></canvas>
    
    <!-- Navbar -->
      <!-- Navbar -->
      <nav class="navbar">
        <div class="nav-container">
            <a href="#" class="logo">ARNI</a>
            <div class="nav-links">
                <a href="/">Home</a>
                <a href="/shop">Shop</a>
                <a href="/men">Men</a>
                <a href="/women">Women</a>
                <a href="/kid">Kids</a>
            </div>
            <div class="nav-right">
                <a href="/login">Login</a>
                <a href="#">🔍</a>
                <a href="#">🛒</a>
            </div>
        </div>

    <div class="container">
        <div class="main-content">
            <div class="sidebar">
                <div class="profile-header">
                    <div class="profile-image-container">
                        <img src="<%= user.profileImage || '/api/placeholder/120/120' %>" alt="Profile">
                    </div>
                    <div>
                        <p style="color: #eab308; font-size: 0.875rem;">Hello,</p>
                        <p style="font-weight: bold; color: #fff;"><%= user.username %></p>
                    </div>
                </div>
                
                <div class="nav-buttons">
                    <a class="nav-button   active" href= "/users/pI" data-page="personal">
                        👤 Personal Information
                </a>
                    <a class="nav-button"  href= "/users/order" data-page="orders">
                        📦 My Orders
                    </a>
                    <button class="nav-button" data-page="wishlist">
                        ♥ My Wishlists
                    </button>
                    <a class="nav-button" href= "/users/adr" data-page="addresses">
                        📍 Manage Addresses
                    </a>
                    <button class="nav-button" data-page="wallet">
                        💰 Wallet
                    </button>
                    <form action="/auth/logout" method="POST">
                        <button type="submit" class="nav-button" id="logoutBtn">
                            🚪 Log Out
                        </button>
                    </form>
                </div>
            </div>

            <div class="content-area">
                <div class="page active" id="personal">
                    <div class="page-header">
                        <h2>Personal Information</h2>
                        <button class="edit-button">✏️ Edit Profile</button>
                    </div>
                    <div class="profile-image-container">
                        <img src="<%= user.profileImage || '/api/placeholder/120/120' %>" alt="Profile" class="profile-image">
                        <button class="change-photo-btn" title="Change Photo">📷</button>
                    </div>
                    <form id="profile-form" class="form-grid">
                        <div class="form-group">
                            <label>Username</label>
                            <input type="text" name="username" value="<%= user.username %>" readonly>
                        </div>
                        <div class="form-group">
                            <label>Email</label>
                            <input type="email" name="email" value="<%= user.email %>" readonly readonly>
                        </div>
                        <div class="form-group">
                            <label>Phone</label>
                            <input type="text" name="phone" value="<%= user.phone %>" readonly readonly>
                        </div>
                        <div class="form-group">
                            <label>Gender</label>
                            <select name="gender" disabled>
                                <option value="Male" <%= user.gender === 'Male' ? 'selected' : '' %>>Male</option>
                                <option value="Female" <%= user.gender === 'Female' ? 'selected' : '' %>>Female</option>
                                <option value="Other" <%= user.gender === 'Other' ? 'selected' : '' %>>Other</option>
                            </select>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <div class="container2">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>About ARNI</h3>
                    <p>Fashion for everyone</p>
                </div>
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <a href="#">Shop</a>
                    <a href="#">About</a>
                    <a href="#">Contact</a>
                </div>
                <div class="footer-section">
                    <h3>Contact Us</h3>
                    <p>Email: info@arni.com</p>
                </div>
                <div class="footer-section">
                    <h3>Follow Us</h3>
                    <div class="social-links">
                        <a href="#">Instagram</a>
                        <a href="#">Facebook</a>
                        <a href="#">Twitter</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Profile Photo Editor Modal -->
    <div class="modal" id="photoEditorModal" style="display: none;">
        <div class="modal-content">
            <h3 class="text-xl font-semibold text-yellow-500">Edit Profile Photo</h3>
            <div class="drop-area" id="dropArea">
                <p>Drag and drop an image here or</p>
                <button id="browseButton" class="text-yellow-500 hover:text-yellow-400">browse files</button>
                <input type="file" id="fileInput" accept="image/*" style="display: none;">
            </div>
            <div class="action-buttons">
                <button class="cancel" id="cancelButton">Cancel</button>
                <button class="save" id="saveButton">Save Changes</button>
            </div>
        </div>
    </div>
   

    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.js"></script>
    <script src="/js/background.js"></script>
    <script src="/javascripts/user/personalInformation.js"></script>
</body>
</html>